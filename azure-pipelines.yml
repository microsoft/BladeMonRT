# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: 'windows-2019'

steps:
- task: gitversion/setup@0
  displayName: 'Install GitVersion'
  inputs:
    versionSpec: '5.6.11'

- task: GitVersion@5
  displayName: 'GitVersion'

- task: Bash@3
  displayName: 'Update version in .nuspec'
  inputs:
    targetType: 'inline'
    script: sed -i "s/1.0.0/$GITVERSIONNUMBER/g" BladeMonRT.nuspec
  env:
    GITVERSIONNUMBER: $(GitVersion.SemVer)

- task: NugetCommand@2
  inputs:
    command: pack
    displayName: 'Nuget pack'
    packagesToPack: '**/*.nuspec'
    packDestination: '$(Build.ArtifactStagingDirectory)'

- task: NuGetAuthenticate@0
  displayName: 'NuGet Authenticate'

- task: NugetCommand@2
  displayName: 'Nuget push'  
  inputs:
    command: push
    packagesToPush: '$(Build.ArtifactStagingDirectory)/**/*.nupkg'
    vstsFeed: 'BladeMonRT/BladeMonRTPipelines'
    publishVstsFeed: 'BladeMonRTPipelines'